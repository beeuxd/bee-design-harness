# Templates — approved page archetypes

Maintained by `recipe-harvest` (page-level harvest). A template is a full-page archetype that shipped: its region layout, hierarchy, and states survived the gates. New pages start from a template when one fits — reuse-first applies at page scale too.

## Template Inventory

| Template | Archetype | Regions (top → bottom) | Recipes used | Responsive notes | Provenance |
|----------|-----------|------------------------|--------------|------------------|------------|
| | dashboard / detail / list / empty-state / auth / settings / marketing | | recipe names from design/recipes.md | 360px base → wider | screen, ship date |

## Starting points

No house template fits → check shadcn blocks (dashboard/sidebar/login archetypes) via the shadcn MCP before designing from zero; imported archetypes graduate here through the gates like everything else.
